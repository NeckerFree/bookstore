import '../index.css';

const Categories = () => (
  <section className="categoriesSection">
    <input id="submit" type="submit" name="checkStatus" value="Check status" />
  </section>
);

export default Categories;
